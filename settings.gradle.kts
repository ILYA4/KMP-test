pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "RecycleMap"
include(":androidApp")
include(":shared-domain")
include(":shared-data")
